{
  "name": "@jamesnimlos/gh-pages",
  "version": "1.0.1",
  "description": "Generates or updates a gh-pages branch from the current directory",
  "engines": {
    "node": ">=v6.6"
  },
  "bin": {
    "gh-pages": "gh-pages-node"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "James Nimlos",
  "license": "MIT",
  "dependencies": {
    "chalk": "^2.3.2",
    "copy": "^0.3.0",
    "cp": "^0.2.0",
    "fs-extra": "^5.0.0",
    "prompt": "^1.0.0",
    "prompt-promise": "^1.0.3",
    "prompt-sync": "^4.1.4",
    "yargs": "^11.0.0"
  }
}
